import React from "react";
import { View } from "react-native";
import { ProfileProps } from "../../../types/NavigationTypes";

export const Profile: React.FC<ProfileProps> = () => {
  return (
    <View>
      
    </View>
  );
};